
<%= link_to "回到首頁",ab_index_path %>
<%= form_for @ab, :url => ab_path(@ab), :method => :patch do |form|%>
	<%= render :partial => 'form', :locals => { :form => form } %>	
	<%= form.submit "更新" %>
		
<% end %>

